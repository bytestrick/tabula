/**
 * Represents a possible data type for cells or columns.
 *
 * @property id
 *   Numeric identifier of the data type.
 * @property name
 *   Human-readable name of the data type.
 */
export interface DataTypeDTO {
  id: number,
  name: string
}
